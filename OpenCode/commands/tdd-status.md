---
description: Show DevBlock TDD session status
---

Call `devblock_status`.

Report the current TDD state and suggest what to do next based on the phase.

If a session is active, include:
- current feature
- current phase
- scoped implementation files
- scoped test files
- configured test command
- the next recommended action

If there is no active session, say so clearly.
